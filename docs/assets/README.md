# Assets (иконки)

Иконки складываем в `public/icons/`.

В коде используем абсолютный путь от корня сайта, например:

- `/icons/icon-search/icon-search-sm.svg`

Правила:

- предпочтительно SVG
- имена файлов — `kebab-case`
