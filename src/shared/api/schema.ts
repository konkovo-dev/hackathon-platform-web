/**
 * Этот файл генерируется автоматически из OpenAPI схемы.
 * Для генерации выполните: pnpm api:gen
 * Убедитесь, что переменная окружения OPENAPI_URL установлена.
 */

// Заглушка до первой генерации
export type paths = Record<string, never>
